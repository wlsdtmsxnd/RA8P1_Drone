# RA8P1 飞控迁移状态

## 迁移基线

- 目标机架与 `RT1064_Drone_10inch` 相同。
- `RT1064_Drone_10inch` 提供已经验证的 IMU 安装方向、CRSF、数传、
  电机顺序和安全逻辑。
- `D:\Renesas\old_fly` 的 RA6M5 工程仅作为完整四轴控制流程参考，
  不复制其 FSP 生成文件和针对小机架的 PID 参数。

## 硬件映射

| 功能 | RA8P1 引脚 | 外设 |
|---|---|---|
| ICM42688 SCK | P415 | SPI1 RSPCK |
| ICM42688 MOSI | P708 | SPI1 MOSI |
| ICM42688 MISO | P709 | SPI1 MISO |
| ICM42688 CS | P710 | GPIO，低有效 |
| CRSF RX / TX | P602 / P603 | SCI0，420000 baud |
| 3DR 数传 RX / TX | P802 / P801 | SCI2，57600 baud |
| Holybro GPS RX / TX | P715 / P714 | SCI4，115200 baud |
| Holybro GPS compass SDA / SCL | P514 / P515 | IIC2 |
| M1 / M2 | P700 / P701 | GPT5 A / B |
| M3 | P109 | GPT10 A |
| M4 | P702 | GPT6 A |

电机顺序沿用 RT1064 实机记录：M1 左前、M2 右前、M3 右后、M4 左后。
四路 ESC PWM 为 50 Hz，范围 1000-2000 us，上电和任何故障状态均保持
1000 us。

## 已完成

- ICM42688 SPI 驱动、500 Hz Mahony 姿态解算和静止陀螺零偏标定。
- 同步 RT1064 实测 FRD 轴向：X 前、Y 右、Z 下；加速度比力取反后参与
  重力校正。
- CRSF 16 通道解析、300 ms 失联判定和 3DR/VOFA JustFloat 数传。
- 四路 GPT PWM、电机脉宽限幅和上电安全输出。
- CH5 解锁：启动后必须先看到低档；CH3 低于 1050 us 且 CH5 高档保持
  1 秒才进入 ARMED。CH5 低、遥控失联或 IMU 更新失败立即停机。
- 编译期保护的电调行程校准和 M1-M4 无桨顺序测试。
- 无桨顺序测试必须由低油门下 CH5 低到高主动触发；CH5 低、油门抬高或
  遥控失联都会立即恢复 1000 us。台架模式可通过 VOFA 观察四路命令、
  当前阶段、活动电机和输出错误。
- 纯摇杆混控波形测试已通过：Roll 右打增加 M1/M4，Pitch 后拉增加
  M1/M2，Yaw 右打增加 M2/M4；反向操作对应关系相反。测试仅验证
  混控矩阵，未接入姿态或陀螺反馈。
- 仅 IMU 回水平角度反馈测试已通过：Roll/Pitch 修正斜率约为
  -1.5 us/deg，四个倾斜方向均生成反向回水平修正；该阶段未接入
  角速度内环或 Yaw 控制。
- 仅 IMU 角速度阻尼测试已通过：X/Y/Z 修正斜率约为 -0.25 us/(deg/s)，
  三轴正反方向均产生反向阻尼；静止有效段四路差值最大 1 us，撤防后
  四路恢复 1000 us。
- Roll/Pitch 保守串级自稳波形测试已通过：实测修正约为角度项
  -0.75 us/deg 与角速度项 -0.25 us/(deg/s) 之和，正反倾斜均正确，
  有效测试段未发生保护误停。
- 受限遥控目标角度串级波形测试已通过：Roll/Pitch 指令均覆盖正负满杆，
  实测指令增益约 7.5 us/单位，所有强指令样本的混控方向均正确。
- 受限 Yaw 目标角速度波形测试已通过：Yaw 指令覆盖正负满杆，实测指令
  增益约 7.47 us/单位，正反方向混控正确且安全状态正常撤防。
- 统一三轴控制路径的断电综合波形测试已通过：Roll/Pitch 两组半杆组合的
  电机叠加关系正确，补测 Yaw 正负半杆和满杆均正确，测试期间无保护误停。
- 已在用户明确确认拆桨、固定机架和周围净空后启用专用无桨通电综合模式；
  该模式带独立 ESC 动力授权、输出软启动和更保守的目标及保护限幅。
- 无桨通电记录的控制器侧检查已通过：三次启动斜率均为 500 us/s，撤防在
  下一遥测帧恢复四路 1000 us，三轴指令差动方向正确，最高输出 1159 us。
- 无桨通电机械响应已通过：四个电机均能稳定启动并在撤防后立即停转，
  Roll/Pitch/Yaw 六个摇杆方向和四个轻微倾斜方向的实际快慢变化均符合
  预期，未观察到抖动、异响、失步或机架松动。
- 统一控制器影子运行已通过：有效记录约 107.04 秒，三轴正负摇杆动作的
  指令/修正符号一致率均为 100%，撤防下一遥测帧全部清零；示波器确认
  四路实际 GPT 全程为 50 Hz、1000 us。
- 已新增独立 `pid_controller` 内核，具备显式周期、输出/积分限幅、测量值
  微分滤波、条件积分抗饱和和统一复位。当前五个姿态控制器仍保持
  `Ki=Kd=0`，没有复制 RA6M5 参数，也没有启用实际电机输出。
- P-only PID 内核等效性复测已通过：三轴正负满杆修正约为正负 6 us，
  指令/修正符号一致率 100%；补测四个机架倾斜方向的测量反馈全部正确，
  非 ARMED 状态严格清零且计算目标未触及限幅。
- 已加入独立的角速度积分影子模式，仅该模式使用 `Ki=0.02` 和正负 2 us
  积分限幅，并通过专用遥测发布三轴积分状态；实际 PWM 仍强制 1000 us。
- 极小角速度积分影子测试已通过：Roll 正/反累积斜率分别为
  +0.478/-0.482 us/s，与理论正负 0.48 us/s 一致；积分在正负 2 us 精确
  限幅，反向可退出限幅并穿零，撤防下一帧全部清零。Pitch/Yaw 积分漂移
  仅约 -0.050/-0.001 us，未发现轴间串扰。
- 完成影子测试后已恢复 `CONTROL_BENCH_MODE_DISABLED`、安全确认 `0U` 和
  正常遥测源；当前默认固件即使 ARMED 也不会把 PID 结果送入电机。
- 正式 P-only PID 无桨通电复测的数据侧检查已通过：五次启动斜率为
  497.2-500.0 us/s，四路启动差不超过 1 us；两次撤防均在下一遥测帧
  回到 1000 us。六个摇杆方向和右侧压低、机头抬高的反馈方向正确，
  最高输出 1192 us，未触及 1280 us 限幅。
- 正式 P-only PID 无桨通电复测的机械侧检查也已由用户确认全部通过：
  四电机同步稳定启动，无延迟或停转；CH5 撤防后全部立即停转；六个摇杆
  方向及右侧压低、机头抬高的实际快慢变化正确；无抖动、异响、失步或
  机架松动。测试后已恢复默认安全配置。

## 尚未完成

- RA8P1 已迁移双环控制结构，专用 P-only 无桨通电模式已通过；但默认
  正常模式仍禁用控制台架路径，即使 ARMED 四路也保持 1000 us。该状态
  是有意保留的安全门，不是故障，也不是可飞控制模式。
- `Ki=0.02` 只验证了软件积分行为，并不是已确认的飞行参数；正式通电
  复测保持 `Ki=Kd=0`，测试结束后已恢复默认安全固件。
- 当前控制器仅使用保守的角度 P 外环和角速度 P 内环，不包含 Yaw 航向
  保持、积分项、微分项或经过推力闭环验证的可飞参数。
- 尚未进行带桨推力测试、系留测试、首次离地测试和实际 PID 调参；不得因
  无桨测试通过而判定项目已经可以带桨飞行。
- RA6M5 的机架尺寸和动力系统不同，其 PID 数值不能直接使用。
- 定高、光流、低电压和传感器冗余保护尚未迁移。

## 下一阶段复核（2026-08-18）

- 不再把无桨、ESC 断电的独立 D 项影子测试列为下一阶段，也不新增该模式。
  当前测量值微分的符号、首样本处理和统一复位路径可由源码确认；断电手动
  转动不能产生电机/桨叶振动、机架共振、推力延迟或真实闭环响应，不能用于
  确定可飞 `Kd` 和滤波参数。
- 已完成的 I 影子测试继续只作为 PID 内核状态管理证据，不再据此推进通电
  `Ki`；当前 `Ki=Kd=0` 的安全状态不变。
- 下一项软件准备应服务于真实桨载数据：明确区分正常飞行输出和历史台架
  模式，并能够同步记录实际四路输出、三轴滤波陀螺和安全状态。默认固件
  仍保持所有电机 1000 us。
- 上述第一项软件准备已完成：新增默认禁用、三重确认、与其他台架模式互斥
  的桨载振动基线模式。它不调用 PID/混控，只输出四路等值、最高 1180 us、
  500 us/s 缓升的脉宽，并以 100 Hz 记录 M1-M4、三轴 40 Hz 带限角速度和
  安全状态。当前模式及全部确认位仍为 `DISABLED/0U`，没有获准硬件运行。
- 已根据包装照片记录候选桨叶：GEMFAN 1045、CF Nylon、10 英寸双叶、
  单包 1 CCW + 1 CW。尚需确认至少两包四只实物、外观和中心孔匹配，以及
  M1-M4 本机实际旋向、桨叶位置和紧固；桨叶确认位继续保持 `0U`。
- 任何安装桨叶或接通 ESC 动力的测试仍需用户另行明确确认。获准后先做固定
  机架的低能量机械/振动/动力基线，再做受限动态调参；调参顺序为角速度
  内环 P、按实际需要加入 D、最后 I，角度外环在内环可控后再调。
- 2026-08-18 用户已确认固定工装、桨叶和现场隔离/紧急断电条件，
  并授权本次桨载振动基线测试安装桨叶、接通 ESC 动力。专用固件已转为
  `VIBRATION_BASELINE/1U/1U/1U`；授权仅限固定工装低能量基线，
  不包含系留、离地或飞行，完成后需恢复默认安全固件。
- 首次固定工装桨载基线已完成：四路全程等值 1000-1180 us，实测
  M1/M2 在 1142 us 起转、M3 在 1147 us 起转、1150 us 时四路均已转动。
  1180 us 保持约 9.19 秒，40 Hz 带限三轴角速度均远低于保护值，
  撤防后四路恢复 1000 us。本结果不是离地或可飞 PID 证据。
- 基线测试结束后已恢复桨载模式 `DISABLED`、三项确认位 `0U`、
  ESC/控制台架模式禁用和正常 RC 遥测。

## 当前首次系留短跳阶段（2026-08-18）

- 用户决定跳过重复的无桨硬件验证，并已确认系留现场条件；新增独立的
  `TETHERED_FLIGHT_MODE_FIRST_HOP` 和系留、桨叶、隔离/停机、ESC 动力
  四项编译期确认位，且与所有历史台架/桨载模式互斥。
- 首次参数为 P-only：角度外环 Kp=0.40；Roll/Pitch 角速度内环 Kp=0.22，
  Yaw 角速度内环 Kp=0.20；全部 Ki=Kd=0。目标倾角正负 3 度，Yaw 目标
  正负 10 deg/s。第一轮 1400 us 未离地且记录未见限幅或保护触发，第二轮
  基础油门最高提高至 1450 us，总输出最高 1470 us。
- 基础油门以 200 us/s 缓升；500 Hz 姿态差动不经过慢斜坡。10 度倾角或
  100 deg/s 角速度保护触发后锁存停机，必须先将 CH5 拨低才能重新解锁。
- 专用遥测为 17 通道、50 Hz，记录实际 M1-M4、基础油门、Roll/Pitch、
  三轴角速度、三轴修正、归一化油门、控制有效标志、安全状态和锁存故障码。
- 第三版专用固件 `clean all` 已通过：`text=37844, data=232, bss=22240`；ELF
  SHA256 `E217F09B7E2F46726FF77968B5CA644FDB319BC6A7090A4B6BBEBAED694E3BEF`，
  SREC SHA256 `92532B1FCF6D11F2F970E26762AEF5536FA1B89FE69136BF6275DEE0AF108B4F`。
- 第一轮 1400 us 平台约 1.94 秒，实际四路 1399-1401 us；姿态、角速度和
  三轴修正均远离保护/限幅，撤防下一帧四路回到 1000 us。详细统计见
  `debug_guide.md` 第 21.3 节。
- 1450 us 版本实际在约 1053 和 1199 us 两次触发控制层锁存；第一次记录到
  X/Y 角速度 -227/-119 deg/s，第二次触发样本未被 50 Hz 遥测采到。第三版
  新增 I16 故障码：1 非有限数、2/3 Roll/Pitch 倾角、4/5/6 X/Y/Z 角速度、
  7 GPT 写入错误；故障码保持到 CH5 拨低。
- 当前产物是可驱动电机的专用系留固件，不是默认安全固件；首次只允许一次
  约 5 cm、1 秒以内的中位摇杆短跳，取得 CSV 后再决定调参。

## 当前 IMU 原始异常诊断阶段（2026-08-18）

- 最新记录确认自动停机为 `I16=5`（Y/Pitch 角速度保护），并在撤防、四路
  1000 us 时发现 X/Y 原始链路产生同类孤立大尖峰，因此暂停继续系留测试。
- 系留模式及四项确认位已恢复 `DISABLED/0U`；新增独立
  `IMU_DIAGNOSTIC_MODE_RAW_REREAD`，与全部电机测试模式编译期互斥。
- 诊断模式在滤波前检测绝对角速度、相邻角速度跳变或加速度跳变，触发后
  立即第二次读取完整 12-byte 六轴寄存器，并锁存两次六轴值和全部字节。
  首读仍进入原姿态数据流，复读仅用于诊断，不替换或修正控制样本。
- 遥测改为 51 通道、20 Hz；每个 IMU 周期强制四路 1000 us。链接检查确认
  ELF 只有 `motor_output_all_stop`，没有 `motor_output_set_us`、
  `flight_control_update` 或 `pid_controller_update`。
- 该测试只比较 J-Link 5 V 与主板 4.99 V 下的静置/手动原始数据，禁止接通
  ESC 动力或进行短跳。完整通道和判据见 `debug_guide.md` 第 22 节。
- 主板供电已在 25.172 s 捕获一次确定的单事务异常：首读 Z 陀螺 12287
  counts，立即复读 -7 counts，加速度三轴完全一致，最后三个原始字节不同。
  已将复读升级为永久原始采样守卫：只有复读成功、字节不同且复读恢复正常
  时才替换首读；两次都异常时不替换，继续保留真实运动和原安全保护。
